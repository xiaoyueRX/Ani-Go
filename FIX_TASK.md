# Ani-Go 双修复任务书

## 修复#2: MergeFromSettings 模板键合并缺失 + year=0 空括号
- 文件: `config.go` 约458行 (MergeFromSettings函数)
- 症状: 用户在设置页修改 TV/MOVIE 模板后,已有订阅不会应用新模板(只有新建订阅生效)。另外 year=0 时路径渲染出 `Title (0)` 或 `Title ()` 空括号
- 修复要求:
  1. MergeFromSettings 合并时必须覆盖 TV/MOVIE/其他媒体类型的模板键(逐键对比,非空即更新)
  2. year<=0 时不渲染年份段(无括号无数字),路径形如 `Title/Season N/...`

## 修复#3: 整理结果计数虚报
- 文件: `scheduler.go` 约279行起
- 症状: 整理失败(如源文件损坏/目标写入失败)时仍计入"成功N个文件"推送,用户被误导
- 修复要求: 成功/失败分开计数; 推送文案分别列出; 全失败时不发成功提示只发失败告警

## 约束
- 只改上述两处相关逻辑,不动其他功能
- 改完 `go build ./...` 必须通过
- 若有现成单测框架,为两处修复各补一个用例

## 验收
完成后打印 ANIGOFIXOK 并列出改动文件与关键diff摘要。
