# 明天继续 — WebUI 前端初始化

## 已完成
- Vite + Vue3 + TypeScript 脚手架 (`npm create vite@latest`)
- 依赖已装: vue-router@4, axios, tailwindcss@4, daisyui@5, @tailwindcss/vite

## 待做 (严格按顺序)

### 1. 配置 vite.config.ts
```ts
// 添加 tailwindcss vite 插件
import tailwindcss from '@tailwindcss/vite'
// plugins: [vue(), tailwindcss()]
```

### 2. 重写 src/style.css
清空 Vite 默认样式，只保留：
```css
@import "tailwindcss";
@plugin "daisyui";
```
主题用 DaisyUI dark mode (data-theme="dark")。

### 3. 创建 src/router/index.ts
- `/login` → Login.vue (无需认证)
- `/` → 占位页 (需认证)
- `beforeEach`: 无 token → 跳 `/login`

### 4. 创建 src/utils/request.ts
Axios 实例：
- `baseURL`: `/api`
- 请求拦截器: 从 localStorage 读 token → `Authorization: Bearer <token>`
- 响应拦截器: 收到 401 → 清空 token → 跳 `/login`

### 5. 创建 src/views/Login.vue
- DaisyUI card 居中，data-theme="dark"
- 用户名 + 密码输入框
- POST /api/login → 存 token → 跳转首页
- 错误提示用 DaisyUI alert

### 6. 修改 src/main.ts
挂载 router，global provide axios。

### 7. 修改 index.html
`<html>` 加 `data-theme="dark"`

### 8. 验证
```bash
cd web && npm run build
```
确保无 TS/构建错误。

## 后端接口参考
- `POST /api/login` — body: `{username, password}` → `{token, username, message}`
- `GET /api/me` — header: `Authorization: Bearer <token>` → `{username}`
- `GET /api/health` — 无需认证

## 项目路径
- 后端根目录: `D:\pve\Ani-Go\` (main.go)
- 前端目录: `D:\pve\Ani-Go\web\`
